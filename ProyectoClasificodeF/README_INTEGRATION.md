# Clasificode Frontend Integrado (Fix form scope)

## Pasos
1) cp .env.example .env.local
2) pnpm install
3) pnpm dev
4) http://localhost:3000

## Login
- Si 401, auto-registro en /register y reintento de login.
- Botón "Crear usuario" debajo del login (usa form.getValues()).
